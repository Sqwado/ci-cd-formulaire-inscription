# Livrable — Projet CI/CD Formulaire Inscription

Repository GitHub :
https://github.com/Sqwado/ci-cd-formulaire-inscription

IP publique — Application (Frontend / API / Adminer) :
http://<APP_IP>:3000
http://<APP_IP>:8000
http://<APP_IP>:8080

IP publique — Registry Docker privé (HTTPS) :
https://<REGISTRY_IP>

# Remplacer <APP_IP> et <REGISTRY_IP> après exécution du workflow Deploy
# (résumé affiché dans l'onglet Summary de GitHub Actions).

# Connexion au registry Docker :
#   docker login <REGISTRY_IP>:443
#   Utilisateur : valeur du secret REGISTRY_USERNAME (voir .env.example)
#   Mot de passe  : valeur du secret REGISTRY_PASSWORD (voir .env.example)
