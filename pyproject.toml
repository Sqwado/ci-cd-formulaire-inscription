[project]
name = "ci-cd-formulaire-inscription-api"
version = "0.4.2"
requires-python = ">=3.12"
dependencies = [
    "fastapi",
    "uvicorn[standard]",
    "mysql-connector-python",
    "pyjwt",
    "bcrypt",
]

[project.optional-dependencies]
test = ["pytest>=8.0", "httpx>=0.27"]

[tool.vercel]
entrypoint = "server/serveur.py:app"
