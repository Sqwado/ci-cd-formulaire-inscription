# Copier ce fichier en .env à la racine du projet :
# cp .env.example .env

# --- MySQL (docker compose : services db et server) ---
MYSQL_ROOT_PASSWORD=change_me
MYSQL_DATABASE=ynov_ci
MYSQL_USER=root

# --- React (npm start en local) ---
REACT_APP_SERVER_PORT=8000
